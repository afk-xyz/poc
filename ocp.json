openapi: 3.0.0
info:
  version: 1.0.0
  title: API
  description: OPEN API 3.0

paths:
   /docs/:
     post:
       summary: Uploads a file.
       consumes:
         - multipart/form-data
       parameters:
         - in: formData
           name: upfile
           type: file
           description: The file to upload
